要修改React工程的名字，需要进行以下步骤：

进入React工程的根目录，找到 package.json 文件。
在 package.json 文件中，找到 "name" 字段，将其值修改为新的工程名。
执行 npm install 命令，安装依赖。
如果你使用的是 create-react-app 创建的工程，还需要修改 public 目录下的 index.html 文件中的 title 标签的内容，将其修改为新的工程名。
在命令行中执行 npm start，重新启动应用，查看是否修改成功。
需要注意的是，修改工程名后可能会出现一些不可预知的问题，因此最好备份一下工程文件，以免修改出现问题。